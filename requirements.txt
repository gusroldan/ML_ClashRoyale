# Data manipulation
pandas>=1.3.0
numpy>=1.21.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.0.0

# Machine Learning
scikit-learn>=1.3.0

# Kedro ecosystem
kedro>=0.18.0
kedro-viz>=6.0.0
kedro-datasets>=1.0.0

# Utilities
jupyter>=1.0.0
ipykernel>=6.0.0
ipython>=8.10
jupyterlab>=3.0
notebook

# Version control and data management
dvc[gdrive]>=3.63.0
pydrive2>=1.16.0

# Machine Learning adicional
xgboost>=1.7.0
lightgbm>=3.3.0
umap-learn>=0.5.0

# Orquestación (instalar con constraints en WSL)
# Sugerido para WSL:
#   AIRFLOW_VERSION=3.1.0
#   CONSTRAINT_URL="https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-3.11.txt"
#   pip install "apache-airflow==${AIRFLOW_VERSION}" --constraint "${CONSTRAINT_URL}"
apache-airflow==3.1.0

# Utilidades
imbalanced-learn>=0.10.0

# Reglas de asociación
mlxtend>=0.22.0

# Detección de anomalías
pyod>=1.1.0  # Biblioteca especializada para detección de anomalías

# Clustering avanzado
hdbscan>=0.8.0  # Clustering jerárquico basado en densidad

# Interpretabilidad
shap>=0.42.0  # SHAP values para interpretabilidad de modelos

# Detección de anomalías con autoencoders (opcional)
# Descomentar si quieres usar autoencoders:
# tensorflow>=2.10.0